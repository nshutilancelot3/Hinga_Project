# Copy to backend/.env.test and point at a THROWAWAY database.
# The integration tests wipe every table between tests, so this must never be the
# dev or production database.
TEST_DATABASE_URL=postgresql://user:password@localhost:5432/hinga_test?schema=public

# Optional — tests fall back to safe placeholders if these are omitted.
# JWT_SECRET=test-jwt-secret
