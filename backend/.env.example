DATABASE_URL=postgresql://user:password@localhost:5432/hinga?schema=public
JWT_SECRET=
OPENWEATHERMAP_KEY=
PLANTID_KEY=
PORT=4000

# Optional: used only by `npm run seed` to create the first admin + demo prices.
SEED_ADMIN_EMAIL=admin@hinga.rw
SEED_ADMIN_PASSWORD=
# Integration tests read TEST_DATABASE_URL from backend/.env.test (see .env.test.example).
